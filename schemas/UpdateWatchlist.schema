UpdateWatchlist_api = %{
  schema: => [
  description: "Partially update watchlist for a specific account: change watchlist name, add to the beginning/end of a watchlist, update or delete items in a watchlist. This method does not verify that the symbol or asset type are valid.",
  errors: [
    {"204", "Watchlist updated."},
    {"400", "An error message indicating the validation problem with the request."},
    {"401", "An error message indicating the caller must pass a valid Authorization in the HTTP authorization request header."},
    {"500", "An error message indicating there was an unexpected server error."}
  ],
  name: "Update Watchlist",
  query_parameters: [
    {"accountId", "string", "Account Number."},
    {"watchlistId", "integer", "The ID of the watchlist to be updated."},
    {"Authorization", "string",
     "Supply an access token to make an authenticated request. The format is <code>Bearer &lt;access token&gt;</code>."}
  ],
  response_schema: [],
  rest_action: "PATCH",
  rest_url: "https://api.tdameritrade.com/v1/accounts/\#{accountId}/watchlists/\#{watchlistId}",
  security_schema: [{"oauthsecurity", [{"grantType", "AUTHORIZATION_CODE"}, {"type", "OAUTH2"}]}]
]
  , dict: => %{}
}
